{
  "name": "j-desk",
  "version": "0.1.0",
  "description": "",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": "^22.0.0"
  },
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
    "test": "vitest run",
    "server": "npm run dev -w @j-desk/server",
    "mcp": "npm run dev -w @j-desk/mcp",
    "mcp:token": "npm run token -w @j-desk/mcp"
  },
  "license": "MIT",
  "dependencies": {
    "@j-desk/core": "0.1.0",
    "@fontsource-variable/inter": "^5.3.0",
    "pdfjs-dist": "^5.6.205"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.6",
    "@sveltejs/kit": "^2.9.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "fake-indexeddb": "^6.2.5",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
